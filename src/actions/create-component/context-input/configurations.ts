export const configurations = `All UI elements should be configurable in an object
saved in the database. The component configuration should be retrieved through
a separate query. The developer concerns in the config for both FE ande BE should
be closely adhered to.
` 
